// Package provider defines the provider abstraction for Settla Rail.
//
// Each payment provider (bank, PSP, blockchain network) implements the
// Provider interface. The mock sub-package provides a test/demo implementation.
package provider
