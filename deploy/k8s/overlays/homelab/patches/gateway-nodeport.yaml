# Expose gateway via NodePort for external access from load test machine.
# Access at http://<any-node-ip>:30080
apiVersion: v1
kind: Service
metadata:
  name: gateway
spec:
  type: NodePort
  ports:
    - name: http
      port: 3000
      targetPort: http
      nodePort: 30080
      protocol: TCP
