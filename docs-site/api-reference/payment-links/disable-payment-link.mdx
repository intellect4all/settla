---
title: "Disable Payment Link"
openapi: "DELETE /v1/payment-links/{id}"
---

Disable a payment link, preventing further redemptions. This is a soft-delete — the link still exists and can be retrieved, but cannot be redeemed.

Returns `204 No Content` on success.
