---
title: "List Deposit Sessions"
openapi: "GET /v1/deposits"
---

List all crypto deposit sessions for your tenant with offset-based pagination. Results are ordered by creation time (newest first).

```bash
curl "https://sandbox.settla.io/v1/deposits?limit=50&offset=0" \
  -H "Authorization: Bearer $API_KEY"
```
